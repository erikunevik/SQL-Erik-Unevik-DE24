CREATE SCHEMA IF NOT EXISTS refined;

CREATE TABLE IF NOT EXISTS refined.strings ( 

--- Skapar tabell

CREATE TABLE IF NOT EXISTS refined.strings ( 
    sql_word VARCHAR(255),
    description VARCHAR(255),
    example VARCHAR(255)
);


SELECT * FROM staging.strings;

DESCRIBE refined.strings;

DESCRIBE staging.strings;

SELECT * FROM refined.strings;







